//makes it easier to create objects
export class Book {
  //defines the object
  constructor(title, author) {
    this.title = title;
    this.author = author;
  }
  // //method - a function
  // greet() {
  //   console.log(`Hello, I am ${this.firstName} and I like ${this.hobby}`);
  // }
}
